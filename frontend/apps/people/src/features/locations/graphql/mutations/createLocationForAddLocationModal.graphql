mutation createLocationForAddLocationModal($input: CreateLocationInput!) {
    createLocation(input: $input) {
        location {
            id
            name
            country {
                id
            }
        }
    }
}