mutation createPolicyForPoliciesList($input: CreatePolicyInput!) {
    createPolicy(input: $input) {
        policy {
            id
            title
            file {
                id
                url
            }
            publicationDate
        }
    }
}