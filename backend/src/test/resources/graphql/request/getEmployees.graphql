query getEmployees($input: EmployeesInput) {
    employees(input: $input) {
        items {
            uuid
            firstName
            lastName
            role
            birthday
            position
            location
        }
        pageInfo {
            currentPage
            totalPages
        }
        totalItems
    }
}