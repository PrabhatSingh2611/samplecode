query getEmployees($input: EmployeesInput) {
    employees(input: $input) {
        items {
            uuid
            firstName
            lastName
            role
            birthday
            reportingManager
            position
            location
        }
        pageInfo {
            currentPage
            totalPages
        }
        totalItems
        }
}