query {
    assetTypeCategories(input: {pagination: {itemsPerPage: 2, pageNumber: 2}}){
        items{
            id
            name
        }
        pageInfo {
            currentPage
            totalPages
        }
        totalItems
    }
}


