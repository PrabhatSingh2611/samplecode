query {
    assetTypeCategories(input: {pagination: {itemsPerPage: 2, pageNumber: 1}}){
        items{
            id
            name
        }
        pageInfo {
            currentPage
            totalPages
        }
        totalItems
    }
}


