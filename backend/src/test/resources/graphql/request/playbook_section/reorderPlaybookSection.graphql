mutation ReorderPlaybookSection($input: ReorderPlaybookSectionInput!) {
    reorderPlaybookSection(input: $input) {
        playbook {
            sections {
                id
                name
            }
        }
    }
}