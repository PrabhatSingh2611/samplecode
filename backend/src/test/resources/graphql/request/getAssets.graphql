query getAssets($input: AssetInput) {
	assets(input: $input) {
        items {
            uuid
            title
            serial
            type {
                uuid
                title
                icon
            }
            employee {
                uuid
                firstName
                lastName
                role
                birthday
                position
                location
            }
            archivedDate
            purchasedDate
        }
        pageInfo {
            currentPage
            totalPages
        }
        totalItems
    }
}