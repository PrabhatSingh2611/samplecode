query {
    leaveTypes(input: {
        pagination: {}
        sort: [name_DESC]
    })
    {
        items {
            id
            name
            days
            endOfYearAction
        }
        pageInfo {
            currentPage
            totalPages
        }
        totalItems
    }
}