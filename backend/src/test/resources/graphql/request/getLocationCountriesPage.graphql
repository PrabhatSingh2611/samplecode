query {
    locationCountries(input: {pagination: {itemsPerPage: 2, pageNumber: 1}}){
        items{
            id
            name
            iconName
        }
        pageInfo {
            currentPage
            totalPages
        }
        totalItems
    }
}


