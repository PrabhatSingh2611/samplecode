query getAssetsType {
    assetTypes{
        items {
            uuid
            title
            icon
        }
        pageInfo {
            currentPage
            totalPages
        }
        totalItems
    }
}