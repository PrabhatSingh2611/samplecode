query getAssetsType {
    assetTypes{
        items {
            id
            title
            icon
        }
        pageInfo {
            currentPage
            totalPages
        }
        totalItems
    }
}