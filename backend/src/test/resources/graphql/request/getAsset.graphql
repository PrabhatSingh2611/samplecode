query getAsset($uuid: UUID!) {
	asset(uuid: $uuid) {
    uuid 
    title
    serial
    type {
      uuid
      title
      icon
    }
    employee {
      uuid
      firstName
      lastName
      role
      birthday
      position
      location
    }
    archivedDate
    purchasedDate
  }
}