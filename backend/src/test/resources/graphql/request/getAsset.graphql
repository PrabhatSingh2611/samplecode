query getAsset($id: Int!) {
	asset(id: $id) {
    uuid 
    title
    serial
    type {
      uuid
      title
      icon
    }
    employee {
      uuid
      firstName
      lastName
      role
      birthday
      position
      location
    }
    archivedDate
    purchasedDate
  }
}