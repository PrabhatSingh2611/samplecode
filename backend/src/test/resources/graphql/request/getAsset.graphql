query getAsset($id: Int!) {
	asset(id: $id) {
    uuid 
    title
    serial
    type {
      title
      icon
    }
    employee {
      uuid
      firstName
      lastName
      role
      birthday
      position
      location
    }
    archivedDate
    purchasedDate
  }
}