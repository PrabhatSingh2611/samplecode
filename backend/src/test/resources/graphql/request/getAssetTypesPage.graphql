query {
    assetTypes(input: {pagination: {itemsPerPage: 2, pageNumber: 2}}){
        items{
            id
            name
            iconName
            createdAt
            category {
                id
                name
            }
        }
        pageInfo {
            currentPage
            totalPages
        }
        totalItems
    }
}