query {
    locations(input: {pagination: {itemsPerPage: 2, pageNumber: 2}}){
        items{
            id
            name
            country {
                id
                name
                iconName
            }
        }
        pageInfo {
            currentPage
            totalPages
        }
        totalItems
    }
}


